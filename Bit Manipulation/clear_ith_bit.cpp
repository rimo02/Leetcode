#include <iostream>
#include <vector>
using namespace std;

int main()
{
    int n = 42;
    int i = 3;
    // int mask = ~(1 << i);
    // int ans = mask & n;
    // cout << ans;
    cout << (n & 1);
}